import OverlayAdapter from './adapter-overlay/src'
import DynamicAdapter from './dynamic-adapter'

export = {
  'overlay-adapter.dapplet-base.eth': OverlayAdapter,
  'dynamic-adapter.dapplet-base.eth': DynamicAdapter,
}
