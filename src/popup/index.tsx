import * as React from 'react'
import * as ReactDOM from 'react-dom'

import { App } from './App'
import './index.css'

const container = document.getElementById('dapplets-overlay-react-app')

ReactDOM.render(<App />, container)
